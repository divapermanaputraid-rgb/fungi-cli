// FungiCode Risk Classifier — Sprint 0 placeholder
import type { RiskLevel } from "../tools/types.js";

// TODO: Sprint 1 — classify by tool + args heuristics
export function classifyRisk(_toolName: string, _input: unknown): RiskLevel {
  return "medium";
}
